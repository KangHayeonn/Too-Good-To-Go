import React from "react";

interface CartContentWrapper {
	children: React.ReactNode;
}

const CartContentWrapper = () => {
	return <div>CartContentWrapper</div>;
};

export default CartContentWrapper;
